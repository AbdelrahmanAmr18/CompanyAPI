using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;

namespace APIDemo.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class DepartmentController : ControllerBase
    {
        /*
         API Operations crud .. Mapping auto mpper dto 
        create database with aso .net core make relationship using data annotation
        // generic repository is must 
        // auto mapper is additional value (good to added)
         
         */
    }
}
